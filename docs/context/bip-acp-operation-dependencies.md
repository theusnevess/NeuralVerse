# ACP Operation Dependency Registry

The BIP registry defines explicit required and optional dependency roles for
each ACP operation. Curriculum is followed by evidence and knowledge; the five
enrichment branches consume stable upstream references; didactic assembly,
validation, governance, compilation and readiness each require their declared
canonical set in deterministic order.

Dependencies are not inferred from artifacts merely present in workflow state.
