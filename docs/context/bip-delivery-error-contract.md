# Delivery Error Contract

Delivery errors use `code`, `message`, `correlation_id`, `contract_version`, and bounded `details`. Raw SQL, persistence names, stack traces, credentials, and governance evidence are not returned.
