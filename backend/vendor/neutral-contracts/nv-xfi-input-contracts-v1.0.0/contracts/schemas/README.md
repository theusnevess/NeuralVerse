# Schema Foundation

The repository meta-schemas and common primitives support four NV-XFI-M2 domain schemas. JSON Schema is normative; generated language projections are not. Shared metadata is composed into every domain schema.
