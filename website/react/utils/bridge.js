/**
 * NeuralVerse React Island Bridge — website/react/utils/bridge.js
 * ===============================================================
 * NOTE: This file is a documentation stub.
 * The actual bridge implementation is now compiled into the local bundle:
 *   website/dist/react-islands.js
 *
 * The bridge is built from:
 *   react-build/src/bridge.js
 *
 * To rebuild the bundle:
 *   cd react-build && npm run build
 *
 * The bridge is published at runtime via:
 *   window.NeuralVerse.react.bridge.mount(container, Component, props)
 *   window.NeuralVerse.react.bridge.update(container, Component, props)
 *   window.NeuralVerse.react.bridge.unmount(container)
 */
